import styles from "./landing-header.module.scss";
import clsx from "clsx";

const LandingHeader = () => {
  return <header className={clsx(styles.container)}>LandingHeader</header>;
};

export default LandingHeader;
